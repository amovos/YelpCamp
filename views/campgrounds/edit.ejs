<% include ../partials/header %>

<div class="container">
    <div class="row">
        <header class="jumbotron">
           <h1 style="text-align: center;">EDIT <%= campground.name %></h1> 
        </header>
        
        <div style="width:30%; margin: 0 auto;">
            <form action="/campgrounds/<%= campground._id %>?_method=PUT" method="POST">
                <div class="form-group">
                    <label for="name">Campground Name</label>
                    <input class="form-control" type="text" name="Campground[name]" id="name" placeholder="e.g. Woodlarks Campsite" value="<%= campground.name %>">
                </div>
                <div class="form-group">
                    <label for="price">Price per Night (£)</label>
                    <input class="form-control" type="number" name="Campground[price]" id="price" placeholder="e.g. 15" value="<%= campground.price %>" min="0.01" step="0.01">
                </div>
                <div class="form-group">
                    <label for="location">Location</label>
                    <input class="form-control" type="text" name="Campground[location]" id="location" placeholder="e.g. Woodlarks Campsite, Farnham, Surrey" value="<%= campground.location %>">
                </div>
                <div class="form-group">
                    <label for="image">Image URL</label>
                    <input class="form-control" type="text" name="Campground[image]" id="image" placeholder="e.g. https://images.unsplash.com/..." value="<%= campground.image %>">
                </div>
                <div class="form-group">
                    <label for="image">Description</label>
                    <input class="form-control" type="text" name="Campground[description]" id="description" placeholder="e.g. A fully accessible campsite with an outdoor heated pool and a zipline!" value="<%= campground.description %>">
                </div>   
                <div class="form-group">
                    <button class="btn btn-lg btn-primary btn-block">Submit!</button>
                </div>                     
            </form>
            <a href="javascript:history.back()">Go Back</a>
        </div>
    </div>
</div>

<% include ../partials/footer %>